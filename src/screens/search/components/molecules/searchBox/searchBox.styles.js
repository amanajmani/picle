import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  root: {
    flexDirection: 'row',
    borderWidth: 1,
    borderColor: '#000',
    borderRadius: 40,
    alignItems: 'center',
    margin: 20,
  },
  inputStyle: {
    flex: 1,
  },
  icon: {
    padding: 10,
  },
});

export default styles;
