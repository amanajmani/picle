import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  passwordContainer: {
    marginTop: 50,
    flexDirection: 'row',
    borderWidth: 1,
    borderColor: '#000',
    borderRadius: 40,
    alignItems: 'center',
  },
  inputStyle: {
    flex: 1,
  },
  icon: {
    padding: 10,
  },
});

export default styles;
